"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { Calculator, Plus, Trash2 } from "lucide-react";

type Project={_id:string;name:string;budget:number}; type Item={_id:string;itemNo:string;description:string;category:string;unit:string;quantity:number;unitCost:number};
const categories=["Materials","Labor","Equipment","Other"]; const money=(n:number)=>new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP",maximumFractionDigits:0}).format(n); const initial={itemNo:"",description:"",category:"Materials",unit:"pcs",quantity:"1",unitCost:"0",notes:""};
export default function BOQPage(){const[projects,setProjects]=useState<Project[]>([]);const[projectId,setProjectId]=useState("");const[items,setItems]=useState<Item[]>([]);const[form,setForm]=useState(initial);const[open,setOpen]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState("");useEffect(()=>{fetch("/api/projects").then(r=>r.json()).then(d=>{setProjects(Array.isArray(d)?d:[]);if(d[0])setProjectId(d[0]._id)}).catch(()=>setError("Could not load projects."))},[]);useEffect(()=>{if(!projectId)return;setLoading(true);fetch(`/api/boq?projectId=${projectId}`).then(r=>r.json()).then(d=>setItems(Array.isArray(d)?d:[])).catch(()=>setError("Could not load BOQ items.")).finally(()=>setLoading(false))},[projectId]);const total=useMemo(()=>items.reduce((s,i)=>s+i.quantity*i.unitCost,0),[items]);const byCategory=useMemo(()=>categories.map(category=>({category,total:items.filter(i=>i.category===category).reduce((s,i)=>s+i.quantity*i.unitCost,0)})),[items]);const selected=projects.find(p=>p._id===projectId);async function addItem(e:FormEvent){e.preventDefault();if(!projectId)return;const r=await fetch("/api/boq",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,projectId})});if(r.ok){const item=await r.json();setItems(p=>[...p,item]);setForm({...initial,itemNo:String(items.length+2)});setOpen(false)}else setError("Could not save BOQ item.")}async function removeItem(id:string){const r=await fetch(`/api/boq/${id}`,{method:"DELETE"});if(r.ok)setItems(p=>p.filter(i=>i._id!==id));else setError("Could not delete BOQ item.")}
return <main className="p-5 md:p-8"><div className="mx-auto max-w-[1500px]"><div className="flex flex-col justify-between gap-4 border-b border-slate-200 pb-5 md:flex-row md:items-end"><div><div className="text-[10px] font-bold uppercase tracking-[.16em] text-blue-600">Estimating Worksheet</div><h1 className="mt-1 text-2xl font-bold tracking-tight">Bill of Quantities</h1><p className="mt-1 text-sm text-slate-500">Prepare quantities, unit rates and the planned project cost.</p></div><button disabled={!projectId} onClick={()=>setOpen(true)} className="flex items-center justify-center gap-2 bg-blue-600 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-40"><Plus size={16}/> Add BOQ Item</button></div>{error&&<div className="mt-5 border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<div className="mt-5 flex flex-col gap-3 border border-slate-200 bg-white p-4 sm:flex-row sm:items-center"><span className="text-[10px] font-bold uppercase tracking-wider text-slate-500">Project</span><select value={projectId} onChange={e=>setProjectId(e.target.value)} className="w-full border border-slate-300 px-3 py-2.5 text-sm outline-none focus:border-blue-600 sm:max-w-xl"><option value="">Select a project</option>{projects.map(p=><option key={p._id} value={p._id}>{p.name}</option>)}</select></div><div className="mt-5 grid grid-cols-2 border border-slate-200 bg-slate-200 md:grid-cols-5"><Stat label="BOQ Estimate" value={money(total)}/>{byCategory.map(x=><Stat key={x.category} label={x.category} value={money(x.total)}/>)}</div>{selected&&<div className="mt-4 flex flex-wrap gap-x-8 gap-y-2 border border-slate-200 bg-white px-5 py-4 text-sm"><span><span className="text-slate-500">Budget </span><b>{money(selected.budget)}</b></span><span><span className="text-slate-500">BOQ / Budget </span><b>{selected.budget?(total/selected.budget*100).toFixed(1):"0.0"}%</b></span><span><span className="text-slate-500">Remaining </span><b className={selected.budget-total<0?"text-red-600":"text-emerald-700"}>{money(selected.budget-total)}</b></span></div>}<section className="mt-5 overflow-x-auto border border-slate-200 bg-white">{loading?<div className="p-12 text-center text-sm text-slate-500">Loading BOQ...</div>:<table className="data-table min-w-[950px]"><thead><tr><th>Item No.</th><th>Description</th><th>Category</th><th>Unit</th><th className="text-right">Quantity</th><th className="text-right">Unit Rate</th><th className="text-right">Amount</th><th></th></tr></thead><tbody>{items.map(i=><tr key={i._id}><td className="font-mono text-xs">{i.itemNo}</td><td className="font-semibold text-slate-900">{i.description}</td><td>{i.category}</td><td>{i.unit}</td><td className="text-right tabular-nums">{i.quantity}</td><td className="text-right tabular-nums">{money(i.unitCost)}</td><td className="text-right font-bold tabular-nums">{money(i.quantity*i.unitCost)}</td><td className="text-right"><button onClick={()=>removeItem(i._id)} className="p-1.5 text-slate-400 hover:text-red-600"><Trash2 size={15}/></button></td></tr>)}</tbody>{items.length>0&&<tfoot><tr><td colSpan={6} className="bg-slate-50 px-4 py-4 text-right text-xs font-bold uppercase tracking-wide">Total Estimated Cost</td><td className="bg-slate-50 px-4 py-4 text-right font-bold">{money(total)}</td><td className="bg-slate-50"/></tr></tfoot>}</table>}{!loading&&items.length===0&&<div className="p-12 text-center text-sm text-slate-500">No BOQ items recorded for this project.</div>}</section>{open&&<div className="fixed inset-0 z-[70] flex items-center justify-center bg-slate-950/40 p-4"><form onSubmit={addItem} className="max-h-[90vh] w-full max-w-2xl overflow-y-auto border border-slate-200 bg-white p-6 shadow-2xl"><div className="mb-5 border-b border-slate-200 pb-4"><div className="text-[10px] font-bold uppercase tracking-wider text-blue-600">Estimate Line Item</div><h2 className="mt-1 text-lg font-bold">Add BOQ Item</h2></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Item No." value={form.itemNo} onChange={v=>setForm({...form,itemNo:v})}/><Field label="Description" required value={form.description} onChange={v=>setForm({...form,description:v})}/><label><span className="text-xs font-bold uppercase tracking-wide text-slate-500">Category</span><select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="mt-1.5 w-full border border-slate-300 px-3 py-2.5 text-sm">{categories.map(c=><option key={c}>{c}</option>)}</select></label><Field label="Unit" required value={form.unit} onChange={v=>setForm({...form,unit:v})}/><Field label="Quantity" required type="number" value={form.quantity} onChange={v=>setForm({...form,quantity:v})}/><Field label="Unit Rate" required type="number" value={form.unitCost} onChange={v=>setForm({...form,unitCost:v})}/></div><div className="mt-5 border border-slate-200 bg-slate-50 px-4 py-3 text-right text-sm">Line Amount <b className="ml-2">{money((Number(form.quantity)||0)*(Number(form.unitCost)||0))}</b></div><div className="mt-5 flex justify-end gap-3 border-t border-slate-200 pt-4"><button type="button" onClick={()=>setOpen(false)} className="border border-slate-200 px-4 py-2.5 text-sm font-semibold">Cancel</button><button className="bg-blue-600 px-5 py-2.5 text-sm font-bold text-white">Add Item</button></div></form></div>}</div></main>}
function Stat({label,value}:{label:string;value:string}){return <div className="bg-white p-4"><div className="text-[10px] font-bold uppercase tracking-wider text-slate-500">{label}</div><div className="mt-1.5 text-base font-bold tabular-nums md:text-lg">{value}</div></div>}
function Field({label,value,onChange,type="text",required=false}:{label:string;value:string;onChange:(v:string)=>void;type?:string;required?:boolean}){return <label><span className="text-xs font-bold uppercase tracking-wide text-slate-500">{label}{required&&" *"}</span><input required={required} type={type} value={value} onChange={e=>onChange(e.target.value)} className="mt-1.5 w-full border border-slate-300 px-3 py-2.5 text-sm outline-none focus:border-blue-600"/></label>}
