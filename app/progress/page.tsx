"use client";

import { FormEvent, useEffect, useState } from "react";
import { Activity, AlertTriangle, Plus, Trash2 } from "lucide-react";

type Project = { _id: string; name: string; contractAmount: number };
type RecordItem = { _id: string; progressDate: string; percentage: number; milestone: string; notes: string };
type Analysis = { actualTotal: number; physicalProgress: number; financialProgress: number; budgetUtilization: number; progressGap: number; progressStatus: string };
const money = (n: number) => new Intl.NumberFormat("en-PH", { style: "currency", currency: "PHP", maximumFractionDigits: 0 }).format(n);

export default function ProgressPage() {
  const [projects, setProjects] = useState<Project[]>([]), [projectId, setProjectId] = useState(""), [records, setRecords] = useState<RecordItem[]>([]), [analysis, setAnalysis] = useState<Analysis | null>(null), [open, setOpen] = useState(false), [loading, setLoading] = useState(false);
  const [form, setForm] = useState({ progressDate: new Date().toISOString().slice(0, 10), percentage: "0", milestone: "", notes: "" });

  useEffect(() => { fetch("/api/projects").then(r => r.json()).then(p => { setProjects(p); if (p[0]) setProjectId(p[0]._id); }); }, []);
  useEffect(() => { if (!projectId) return; setLoading(true); Promise.all([fetch(`/api/progress?projectId=${projectId}`).then(r => r.json()), fetch(`/api/cost-analysis?projectId=${projectId}`).then(r => r.json())]).then(([r, a]) => { setRecords(r); setAnalysis(a); }).finally(() => setLoading(false)); }, [projectId]);

  async function add(e: FormEvent) { e.preventDefault(); const r = await fetch("/api/progress", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, projectId }) }); if (r.ok) { const x = await r.json(); setRecords(p => [x, ...p]); setAnalysis(a => a ? { ...a, physicalProgress: x.percentage, progressGap: a.financialProgress - x.percentage, progressStatus: a.financialProgress - x.percentage > 10 ? "Spending ahead of physical progress" : a.financialProgress - x.percentage < -10 ? "Physical progress ahead of spending" : "Progress and spending are aligned" } : a); setOpen(false); } }
  async function del(id: string) { const r = await fetch(`/api/progress/${id}`, { method: "DELETE" }); if (r.ok) { setRecords(p => p.filter(x => x._id !== id)); } }

  return <main className="min-h-screen p-5 md:p-8"><div className="mx-auto max-w-7xl">
    <div className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="text-[11px] font-bold uppercase tracking-[0.14em] text-blue-600">Monitoring</div><h1 className="mt-1 text-2xl font-bold tracking-tight">Project Progress</h1><p className="mt-1 text-sm text-slate-500">Track physical work progress against actual project spending.</p></div><button disabled={!projectId} onClick={() => setOpen(true)} className="flex items-center justify-center gap-2 bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-blue-700 disabled:opacity-40"><Plus size={17} /> Record Progress</button></div>
    <div className="card mb-5 p-4"><label className="text-[11px] font-bold uppercase tracking-wider text-slate-500">Project</label><select value={projectId} onChange={e => setProjectId(e.target.value)} className="mt-2 w-full border border-slate-300 bg-white px-3 py-2.5 text-sm sm:max-w-xl"><option value="">Select a project</option>{projects.map(p => <option key={p._id} value={p._id}>{p.name}</option>)}</select></div>
    {loading && <div className="card p-10 text-center text-sm text-slate-500">Loading project control data...</div>}
    {!loading && analysis && <>
      <div className="grid gap-px overflow-hidden border border-slate-200 bg-slate-200 md:grid-cols-2 xl:grid-cols-4"><Metric label="Physical Progress" value={`${analysis.physicalProgress.toFixed(1)}%`} /><Metric label="Financial Progress" value={`${analysis.financialProgress.toFixed(1)}%`} /><Metric label="Budget Utilization" value={`${analysis.budgetUtilization.toFixed(1)}%`} /><Metric label="Actual Cost" value={money(analysis.actualTotal)} /></div>
      <div className={`mt-5 border p-5 ${analysis.progressGap > 10 ? "border-red-200 bg-red-50" : "border-slate-200 bg-white"}`}><div className="flex items-start gap-3">{analysis.progressGap > 10 ? <AlertTriangle className="mt-0.5 text-red-600" size={19} /> : <Activity className="mt-0.5 text-blue-600" size={19} />}<div><div className="text-sm font-bold">{analysis.progressStatus}</div><p className="mt-1 text-xs text-slate-500">Financial progress is {analysis.progressGap >= 0 ? `${analysis.progressGap.toFixed(1)} points ahead of` : `${Math.abs(analysis.progressGap).toFixed(1)} points behind`} physical progress.</p></div></div></div>
    </>}
    {!loading && <div className="card mt-5 overflow-x-auto"><div className="border-b border-slate-200 px-5 py-4"><h2 className="text-sm font-bold">Progress Records</h2></div>{records.length ? <table className="data-table min-w-[750px] text-sm"><thead><tr><th>Date</th><th>Progress</th><th>Milestone</th><th>Notes</th><th></th></tr></thead><tbody>{records.map(x => <tr key={x._id}><td>{new Date(x.progressDate).toLocaleDateString("en-PH")}</td><td className="font-bold">{x.percentage}%</td><td>{x.milestone || "—"}</td><td>{x.notes || "—"}</td><td><button onClick={() => del(x._id)} className="p-1.5 text-slate-400 hover:text-red-600"><Trash2 size={16} /></button></td></tr>)}</tbody></table> : <div className="p-12 text-center text-sm text-slate-500">No progress records yet.</div>}</div>
    {open && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4"><form onSubmit={add} className="w-full max-w-lg bg-white p-6 shadow-xl"><h2 className="text-lg font-bold">Record Project Progress</h2><div className="mt-5 space-y-4"><Field label="Date" type="date" value={form.progressDate} onChange={v => setForm({ ...form, progressDate: v })} /><Field label="Physical Progress (%)" type="number" value={form.percentage} onChange={v => setForm({ ...form, percentage: v })} /><Field label="Milestone" value={form.milestone} onChange={v => setForm({ ...form, milestone: v })} /><label className="block"><span className="text-sm font-medium">Notes</span><textarea value={form.notes} onChange={e => setForm({ ...form, notes: e.target.value })} rows={3} className="mt-1.5 w-full border border-slate-300 px-3 py-2.5 text-sm" /></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setOpen(false)} className="border border-slate-300 px-4 py-2.5 text-sm font-semibold">Cancel</button><button className="bg-blue-600 px-5 py-2.5 text-sm font-semibold text-white">Save Progress</button></div></form></div>}
  </div></main>;
}
function Metric({ label, value }: { label: string; value: string }) { return <div className="bg-white p-5"><div className="text-[11px] font-bold uppercase tracking-wider text-slate-500">{label}</div><div className="mt-2 text-xl font-bold">{value}</div></div>; }
function Field({ label, value, onChange, type = "text" }: { label: string; value: string; onChange: (v: string) => void; type?: string }) { return <label className="block"><span className="text-sm font-medium">{label}</span><input required type={type} min={type === "number" ? 0 : undefined} max={type === "number" ? 100 : undefined} value={value} onChange={e => onChange(e.target.value)} className="mt-1.5 w-full border border-slate-300 px-3 py-2.5 text-sm" /></label>; }
