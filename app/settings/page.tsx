"use client";

import { useState } from "react";
import { Check, Database, Settings } from "lucide-react";

export default function SettingsPage(){const [saved,setSaved]=useState(false),[company,setCompany]=useState({name:"",address:"",engineer:"",contact:""});function save(){localStorage.setItem("constructflow_company",JSON.stringify(company));setSaved(true);setTimeout(()=>setSaved(false),2000)}return <main className="min-h-screen bg-gray-50 p-5 md:p-8"><div className="mx-auto max-w-4xl"><div className="mb-6"><h1 className="text-2xl font-bold">Settings</h1><p className="mt-1 text-sm text-gray-500">Configure company information used for project documentation.</p></div><section className="rounded-xl border border-gray-200 bg-white p-6"><div className="flex items-center gap-3"><div className="rounded-lg bg-gray-100 p-2"><Settings size={19}/></div><div><h2 className="font-bold">Company Information</h2><p className="text-sm text-gray-500">Basic details for reports and records.</p></div></div><div className="mt-6 grid gap-4 md:grid-cols-2"><Field label="Company / Firm Name" value={company.name} onChange={v=>setCompany({...company,name:v})}/><Field label="Project Engineer" value={company.engineer} onChange={v=>setCompany({...company,engineer:v})}/><Field label="Office Address" value={company.address} onChange={v=>setCompany({...company,address:v})}/><Field label="Contact Information" value={company.contact} onChange={v=>setCompany({...company,contact:v})}/></div><div className="mt-6 flex justify-end"><button onClick={save} className="flex items-center gap-2 rounded-lg bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white">{saved?<><Check size={17}/>Saved</>:"Save Settings"}</button></div></section><section className="mt-5 rounded-xl border border-gray-200 bg-white p-6"><div className="flex items-center gap-3"><Database size={19}/><div><h2 className="font-bold">Data Connection</h2><p className="text-sm text-gray-500">Production data is stored in MongoDB through the server API. Keep your connection string in the deployment environment and never expose it in client-side code.</p></div></div></section></div></main>}
function Field({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block"><span className="text-sm font-medium">{label}</span><input value={value} onChange={e=>onChange(e.target.value)} className="mt-1.5 w-full rounded-lg border border-gray-300 px-3 py-2.5 text-sm outline-none focus:border-slate-500"/></label>}
