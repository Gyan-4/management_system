"use client";

import { useEffect, useState } from "react";
import { Check, Database, Settings } from "lucide-react";

const defaultCompany={name:"",address:"",engineer:"",contact:""};
export default function SettingsPage(){const [saved,setSaved]=useState(false),[company,setCompany]=useState(defaultCompany);useEffect(()=>{try{const raw=localStorage.getItem("constructflow_company");if(raw)setCompany({...defaultCompany,...JSON.parse(raw)})}catch{}},[]);function save(){localStorage.setItem("constructflow_company",JSON.stringify(company));setSaved(true);setTimeout(()=>setSaved(false),2000)}return <main className="min-h-screen p-5 md:p-8"><div className="mx-auto max-w-5xl"><div className="mb-6 border-b border-slate-200 pb-5"><div className="text-[10px] font-bold uppercase tracking-[0.16em] text-blue-600">System / Configuration</div><h1 className="mt-1 text-2xl font-bold tracking-tight">Settings</h1><p className="mt-1 text-sm text-slate-500">Configure firm information and system connection details.</p></div><section className="border border-slate-200 bg-white"><div className="flex items-center gap-3 border-b border-slate-200 px-6 py-5"><div className="border border-slate-200 bg-slate-50 p-2"><Settings size={18}/></div><div><h2 className="text-sm font-bold">Company Information</h2><p className="mt-1 text-xs text-slate-500">These details can be used for project documentation.</p></div></div><div className="grid gap-5 p-6 md:grid-cols-2"><Field label="Company / Firm Name" value={company.name} onChange={v=>setCompany({...company,name:v})}/><Field label="Project Engineer" value={company.engineer} onChange={v=>setCompany({...company,engineer:v})}/><Field label="Office Address" value={company.address} onChange={v=>setCompany({...company,address:v})}/><Field label="Contact Information" value={company.contact} onChange={v=>setCompany({...company,contact:v})}/></div><div className="flex justify-end border-t border-slate-200 bg-slate-50 px-6 py-4"><button onClick={save} className="flex items-center gap-2 bg-blue-600 px-5 py-2.5 text-sm font-bold text-white hover:bg-blue-700">{saved?<><Check size={16}/>Saved</>:"Save Settings"}</button></div></section><section className="mt-5 border border-slate-200 bg-white"><div className="flex items-start gap-3 p-6"><Database size={19} className="mt-0.5 text-blue-600"/><div><h2 className="text-sm font-bold">Database Connection</h2><p className="mt-1 text-sm leading-6 text-slate-500">Production records are stored in MongoDB through the server API. Keep <code className="bg-slate-100 px-1.5 py-0.5 text-xs">MONGODB_URI</code> only in the deployment environment. Never expose the connection string in browser code.</p><div className="mt-4 border border-emerald-200 bg-emerald-50 px-4 py-3 text-xs font-semibold text-emerald-800">Connection configuration is server-side.</div></div></div></section></div></main>}
function Field({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block"><span className="text-sm font-semibold text-slate-700">{label}</span><input value={value} onChange={e=>onChange(e.target.value)} className="mt-1.5 w-full border border-slate-300 px-3 py-2.5 text-sm outline-none focus:border-blue-500"/></label>}
